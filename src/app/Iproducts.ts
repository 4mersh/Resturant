export interface IProducts {
        name: string;
        Image:  string;
        price: number;
        discription: string
};

export interface Idrinks {
    title: string;
    img: string;
    price: number;
}